# Copy this file to .env and fill in your keys.
# Variables starting with VITE_ are bundled into the public JS — do not put secrets here.

# (no keys required for basic functionality — Open-Meteo is free and keyless)
